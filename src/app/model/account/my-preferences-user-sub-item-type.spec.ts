import { MyPreferencesUserSubItemType } from './my-preferences-user-sub-item-type';

describe('MyPreferencesUserSubItemType', () => {
  it('should create an instance', () => {
    expect(new MyPreferencesUserSubItemType()).toBeTruthy();
  });
});
